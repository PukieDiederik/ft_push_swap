#include <stdio.h>
#include "stack.h"
#include "push_swap.h"
#include "libft.h"

int main(int argc, char **argv)
{
    t_stack *stack = parse_args(argc - 1, argv + 1);
    int *sorted = to_array
    if (!stack) {
        printf("error\n");
        return (1);
    }
    printf("%d\n", stack->value);
}